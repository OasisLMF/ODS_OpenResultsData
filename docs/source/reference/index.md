# Reference

The authoritative ORD schema. These pages are generated at build time from the `Schema/`
CSVs in this repository — edit the CSVs, not the generated tables.

```{toctree}
:maxdepth: 2

tables
fields
worked-examples
```
