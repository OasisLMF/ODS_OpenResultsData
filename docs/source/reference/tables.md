# ORD tables

The catalogue of result tables defined by ORD, grouped by category. Each table has a
standard acronym used throughout the tooling (e.g. `SELT`, `EPT`, `ALT`). "In Oasis"
indicates whether the table is currently produced by the OasisLMF toolchain.

```{include} _generated/ord_tables.md
```
