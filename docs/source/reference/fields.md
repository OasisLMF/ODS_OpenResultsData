# ORD fields

The full list of ORD fields, grouped by category. **Tables / File** lists the tables each
field appears in, **Format** is the serialisation (`csv` or `json`), and **Current Oasis
name** maps the ORD field to the legacy OasisLMF column name where one exists.

```{include} _generated/ord_fields.md
```
